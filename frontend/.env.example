# Copy this file to .env and adjust for your local environment.
# .env is git-ignored — never commit real values here.

# Port 8001, not the conventional 8000: on this machine, port 8000 is
# already used by an unrelated project (orbit-gtm-qualifier). Adjust to
# match whatever port you actually run the backend dev server on.
VITE_API_BASE_URL=http://localhost:8001/api/v1
