# Loaded automatically by `vite build` (production mode), overriding the
# dev-only absolute URL in .env. Relative on purpose: in any nginx-fronted
# deployment the backend is only reachable through the reverse proxy on the
# same origin as the page itself, never exposed on its own port directly.
VITE_API_BASE_URL=/api/v1
